-- Secrets Configuration Template
--
-- SETUP INSTRUCTIONS:
-- 1. Copy this file to: ~/.hammerspoon/display-profiles-secrets.lua
--    cp display-profiles-secrets.lua.example ~/.hammerspoon/display-profiles-secrets.lua
--
-- 2. Edit the copied file and add your actual secrets
--
-- 3. Set restrictive permissions (user read/write only):
--    chmod 600 ~/.hammerspoon/display-profiles-secrets.lua
--
-- 4. NEVER commit the actual secrets file to version control
--
-- SECURITY NOTES:
-- - This file should ONLY exist in ~/.hammerspoon/ (NOT in this git repo)
-- - The actual secrets file is listed in .gitignore to prevent accidental commits
-- - File permissions (600) ensure only your user account can read/write it
-- - macOS FileVault encryption protects this file at rest
--

local secrets = {}

-- Slack API Token
-- Get this from: https://api.slack.com/apps
-- Required scope: users.profile:write
secrets.slackToken = "xoxp-your-actual-token-here"

-- Future secrets can be added here:
-- secrets.calendarToken = "your-calendar-api-token"
-- secrets.teamsToken = "your-teams-api-token"

return secrets
